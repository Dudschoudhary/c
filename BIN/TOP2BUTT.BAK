#include<stdio.h>
#include<conio.h>
//Top to Buttom Approce//
int c;
void add(int a, int b)
{
      c=a+b;
      printf(" Addition two numbers:-%d\n",c);
}
void subtration(int a, int b)
{
      c=a-b;
//      printf("Subtration is:-%d\n",c);
      if(c<=0)
      {
	printf("Negetive values:-[%d]\n",c);
      }
      else
      {
	printf("Positive subtration values:-%d\n:-",c);
      }
}
void multply(int a, int b)
{
      c=a*b;
      printf("Multplycation is two numberws:-%d\n",c);
}
void main()
{
	int x,y;
	clrscr();
	printf("Enter a value of (X):-");
	scanf("%d",&x);
	printf("Enter a value of (Y):-");
	scanf("%d",&y);
	add(x,y);
	subtration(x,y);
	multply(x,y);
	getch();
}