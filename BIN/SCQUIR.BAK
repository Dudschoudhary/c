#include<stdio.h>
#include<conio.h>
void main()
{
	int i,n,j=0,sum=0,qub=0;
	clrscr();
	printf("Enter a values:-");
	scanf("%d",&n);
	for(i=1;i<=n;i++)//2
	{
		sum=0;
		for(j=1;j<=i;j++)//1,2
		{
			sum = (i*j);
		}
		printf("%d squre is -: %d \t",i,sum);
	}
		printf("\n");
	qub=0;
	for(i=1;i<=n;i++)//2
	{
		for(j=1;j<=i;j++)//1,2
		{
			qub= (i*j*j);
		}
		printf("%d qube is -: %d \n",i,qub);
	}

getch();
}

