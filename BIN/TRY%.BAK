#include<stdio.h>
#include<conio.h>
void main()
{
int i,n,f=0;
clrscr();
printf("enterf az valuyes:-");
scanf("%d",&n);
for(i=2;i<n/2;i++)
{
	if(n%i==0)
	{
		f=1;
		break;
	}
}
if(f==0)
	printf("number is prime.");
else
	peintf("number is not prime");
getch();
}